# @package _group_

_target_: pytorch_lightning.callbacks.ModelCheckpoint
dirpath: ${oc.env:NINJADESC_OUTPUT_ROOT,./outputs}/checkpoints
filename: "{epoch}-{step}"
save_top_k: 3
verbose: False
monitor: "ssim_validation"
mode: 'max'
